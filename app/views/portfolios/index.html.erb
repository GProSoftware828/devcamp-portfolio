<h1>Portfolio Items</h1>

<%= link_to "Create New Item", new_portfolio_url if logged_in?(:user) %>
<div class="album text-muted">
	<div class="container">
		<div class="row sortable">
			<%= render partial: 'portfolio_item', collection: @portfolio_items %>
		</div>
	</div>
</div>